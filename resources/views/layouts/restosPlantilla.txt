  <!-- nav bar -->
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand"></a>
    <a class="navbar-brand" href="{{route('home')}}">
      <img src="{{asset('img/LogoAppMini.jpg')}}" width="30" height="30" class="d-inline-block align-top" alt="Logo">
      Liga de Futbol:
    </a>


    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">
        
        <li class="nav-item active">
          <a class="nav-link" href="{{route('home')}}">Home <span class="sr-only">(current)</span></a>
        </li>

        <!--LOGIN / LOG OUT-->
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Login/logout
          </a>

          <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
            <a class="dropdown-item" href="{{route('register')}}">Register new user</a> 

            <div class="dropdown-divider"></div>

            <a class="dropdown-item" href="{{route('dashboard')}}">Login / logout </a>

            <div class="dropdown-divider"></div>

            <a class="dropdown-item" href="{{route('password.request')}}">Password recovery</a>
          </div>
        </li>

        <!--TEAMS-->
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Teams:
          </a>

          <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
            <a class="dropdown-item" href="{{route('team.show')}}">Show</a> 

            <a class="dropdown-item" href="{{route('team.create')}}">Create</a>
          </div>
        </li>

        <!--MATCHES-->
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Matches:
          </a>

          <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
            <a class="dropdown-item" href="{{route('match.show')}}">Show</a> 

            <a class="dropdown-item" href="{{route('match.create')}}">Create</a>
          </div>
        </li>

        <!--STADIUM-->
      </ul>
    </div>

    <span class="navbar-text">
        <?php
        use Illuminate\Support\Facades\Cookie;

        if (Auth::check()) {
            $usuario=Cookie::get('usuario');
            $rol = Cookie::get('rol');
            echo "Usuario: $usuario - rol: $rol";
      
        } else {
            echo "NO Login :-(";
        }
        ?>
    </span>
  </nav>
  <!-- FIN nav bar -->